type Todo {
    id: ID! @goField(forceResolver: true)
    databaseId: Int!
    text: String!
    done: Boolean!
    user: User!
    query: Query!
    mutation: Mutation!
}

input NewTodo {
    text: String!
    userId: String!
}

input New_Todo {
    i_am_incorrect: Boolean
}